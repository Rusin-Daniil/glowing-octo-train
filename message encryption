
def encryption(words):#создание функции, зашифровывающей введённое сообщение
    cryp_words = ''#создание переменной с пустой строкой, в которую в дальнейшем будет записываться зашифрованное сообщение
    #при условии, что syb является точкой
#в переменную cryp_words добавляется точка
    for syb in words:                                                 
        if syb == '.':
            cryp_words += '.'                                   
 #при условии, что syb является пробелом
#в переменную cryp_words добавляется пробел          
        elif syb == ' ':
            cryp_words += ' '
#при условии, что syb является ё
#в переменную cryp_words добавляется символ стоящий на три позиции дальше буквы е в русском алфавите'''
        elif syb == 'ё':
           cryp_words += chr(ord('е') + 3)
#при условии, что syb является Ё
#в переменную cryp_words добавляется символ стоящий на три позиции дальше буквы У в русском алфавите'''
        elif syb == 'Ё':
            cryp_words += chr(ord('Е') + 3)
#при условии, что syb является э
# переменную cryp_words добавляется а из русского алфавита'''
        elif syb == 'э':
            cryp_words += chr(ord('а'))
#при условии, что syb является Э
#в переменную cryp_words добавляется символ стоящий на три позиции дальше буквы А в русском алфавите'''
        elif syb == 'Э':
            cryp_words += chr(ord('А'))
#при условии, что syb является ю
#в переменную cryp_words добавляется символ стоящий на одну позицию дальше буквы а в русском алфавите'''
        elif syb == 'ю':
            cryp_words += chr(ord('а') + 1)
#при условии, что syb является Ю
#в переменную cryp_words добавляется символ стоящий на одну позицию дальше буквы А в русском алфавите'''
        elif syb == 'Ю':
            cryp_words += chr(ord('А') + 1)
#при условии, что syb является я
#в переменную cryp_words добавляется символ стоящий на две позиции дальше буквы а в русском алфавите'''
        elif syb == 'я':
            cryp_words += chr(ord('а') + 2)
#при условии, что syb является Я
#в переменную cryp_words добавляется символ стоящий на две позиции дальше буквы А в русском алфавите'''
        elif syb == 'Я':
            cryp_words += chr(ord('А') + 2)
            #при условии, что syb является x
#в переменную cryp_words добавляется символ стоящий на три позиции дальше буквы a в английском алфавите'''
        elif syb == 'x':
            cryp_words += chr(ord('a'))
            #при условии, что syb является X
#в переменную cryp_words добавляется А в английском алфавите'''
        elif syb == 'X':
            cryp_words += chr(ord('A'))
#при условии, что syb является y
#в переменную cryp_words добавляется символ стоящий на одну позицию дальше буквы a в английском алфавите'''
        elif syb == 'y':
            cryp_words += chr(ord('a') + 1)
#при условии, что syb является Y
#в переменную cryp_words добавляется символ стоящий на одну позицию дальше буквы A в английском алфавите'''
        elif syb == 'Y':
            cryp_words += chr(ord('A') + 1)
#при условии, что syb является z
#в переменную cryp_words добавляется символ стоящий на две позиции дальше буквы а в английском алфавите'''
        elif syb == 'z':
            cryp_words += chr(ord('a') + 2)
#при условии, что syb является Z
#в переменную cryp_words добавляется символ стоящий на две позиции дальше буквы A в английском алфавите'''
        elif syb == 'Z':
            cryp_words += chr(ord('A') + 2)
#Если же ни  одно из условивй не было выполнено, то символ, записанный в переменную syb, превращается в символ стоящий на три позиции дальшего него(действие может выполняется с любым из двух языков)'''
        else:
            cryp_words += chr(ord(syb) + 3)            
    return cryp_words #возвращение переменной cryp_words, которая содержит зашиврованное сообщение
string = input('Введите сообщение: ') #запрос вводы у пользователя ссобщения, которое требуется засекретить
print('Зашифрованное сообщение:', encryption(string)) #вывод результата функции encryption с передачей в неё
                                                        #переменной string, содержащей сообщение, которое требуется засекретить 

